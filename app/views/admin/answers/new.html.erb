<div class="admincontent container mt-4">
  <h1 class="mb-4"><%= t('.header') %></h1>

  <div class="card mb-4">
    <div class="card-body">
      <p class="card-text">
        <%= @question.body %>
      </p>
    </div>
  </div>

  <%= render 'form' %>

  <div class="mt-3">
    <%= link_to t('.back'), admin_question_path(@question), class: 'btn btn-secondary' %>
  </div>
</div>
