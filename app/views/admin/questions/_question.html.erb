        <tr>
          <td><%= question.body %></td>
          <td><%= link_to t('admin.author_actions.show'), admin_question_path(question), 
                                  method: :get  %></td>
          <td><%= link_to t('admin.author_actions.edit'), edit_admin_question_path(question), method: :get %></td>
          <td><%= link_to t('admin.author_actions.delete'), admin_question_path(question.id), 
                                    method: :delete, 
                                    data: { confirm: t('admin.author_actions.delete_confirmation') } %></td>
        </tr>
