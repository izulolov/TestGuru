        <tr>
          <td><%= question.body %></td>
          <td><%= link_to 'Show', admin_question_path(question), 
                                  method: :get  %></td>
          <td><%= link_to 'Edit', edit_admin_question_path(question), method: :get %></td>
          <td><%= link_to 'Delete', admin_question_path(question.id), 
                                    method: :delete, 
                                    data: { confirm: 'Вы уверены?' } %></td>
        </tr>
