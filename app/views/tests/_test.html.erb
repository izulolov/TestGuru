  <tr>
    <td><%= test.title %></td>
    <td><%= test_level(test) %></td>
    <td><%= test_questions_number(test) %></td>
    <td><%= button_to t('tests.test.start'), start_test_path(test) %></td>
  </tr>
