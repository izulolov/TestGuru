<% if resources.errors.present? %>
    <div>
      <h2><%= pluralize(resources.errors.count, 'error') %> detected:</h2>
      <ul>
        <% resources.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
