<div class="row">
  <div class="d-flex flex-column justify-content-center align-items-center">
    <% flash.each do |key, message| %>
      <%= content_tag :div, message.html_safe, class: "alert #{ flash_type(key) }"%>
    <% end %>
  </div>
</div>
